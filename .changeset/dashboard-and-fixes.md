---
"tiime-cli": minor
"tiime-sdk": patch
---

Add dashboard command with local web server, fix API paths (remove leading slashes for ofetch compatibility), and respect XDG_CONFIG_HOME for config directory.
